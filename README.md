# InstaDelivery-backend

### Scripts

- `npm start`: a script for the production start (or heroku)
- `npm run dev`: runs the server with nodemon
- `npm run build`: a script to build the database using `init.sql`
